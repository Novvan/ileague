# docs/adr

Architecture decision records for the judgment calls made during implementation. First one due: `0001-match-status-model.md`, documenting the single-`matches`-table-with-status-enum decision, written as part of `implementationPlan/02-contracts-domain-model.md`'s E2-T1.

Format: one file per decision, numbered sequentially (`000N-short-title.md`), covering what was decided, why, and what alternatives were rejected and why.
